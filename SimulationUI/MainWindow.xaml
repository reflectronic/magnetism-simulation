<Window x:Class="SimulationUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:HelixToolkit="clr-namespace:HelixToolkit.Wpf;assembly=HelixToolkit.Wpf"
        xmlns:local="clr-namespace:SimulationUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <UniformGrid x:Name="VectorGrid" />
        <HelixToolkit:HelixViewport3D x:Name="Viewport" CameraMode="WalkAround" ClipToBounds="false" ModelUpDirection="0,0,1" ShowFieldOfView="True">
            <ModelVisual3D>
                <ModelVisual3D.Content>
                    <DirectionalLight Color="LightGray" Direction="-0.612372,-0.5,-0.612372" />
                    <!--AmbientLight Color="DarkGray" /-->
                </ModelVisual3D.Content>
            </ModelVisual3D>
            <HelixToolkit:GridLinesVisual3D />
        </HelixToolkit:HelixViewport3D>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="20" Background="#DDFFFFFF">
            <Button Click="Create_Click" x:Name="CreateResourcesButton">Create resources</Button>
            <Button Click="Begin_Click" Margin="5,0,0,0" x:Name="BeginSimulationButton">Begin simulation</Button>
            <Slider ValueChanged="Slider_Changed" Margin="20,0,0,0" x:Name="SpeedSlider" Width="200" Minimum="0" Maximum="20" IsSnapToTickEnabled="True" TickFrequency="1" Value="1" TickPlacement="Both" />
        </StackPanel>
    </Grid>
</Window>
